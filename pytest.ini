[pytest]
; Faza H0 briefu Hyperliquid: testy `listen()` sa asynchroniczne (async def).
; "auto" pozwala pisac `async def test_...():` bez dopisywania
; @pytest.mark.asyncio do kazdego z osobna.
asyncio_mode = auto
